

<TypePrice>:
    orientation: 'vertical'

    BoxLayout:
        size_hint: (1, 0.1)

        Label:
            #size_hint_y: None
            #height: app.menu_height
            text: 'Abs'

        CheckBox:
            id: cb_abs
            group: 'g1'
            active: True
            on_active: root.on_checkbox1_active(self, 'abs')

        Label:
            text: 'Close'

        CheckBox:
            id: cb_close
            group: 'g1'
            on_active: root.on_checkbox1_active(self, 'close')

        Label:
            text: 'Open'

        CheckBox:
            id: cb_open
            group: 'g1'
            on_active: root.on_checkbox1_active(self, 'open')

        Label:
            text: 'Max'

        CheckBox:
            id: cb_max
            group: 'g1'
            on_active: root.on_checkbox1_active(self, 'max')

        Label:
            text: 'Min'

        CheckBox:
            id: cb_min
            group: 'g1'
            on_active: root.on_checkbox1_active(self, 'min')

    BoxLayout:
        size_hint: (1, 0.1)

        Label:
            #size_hint_y: None
            #height: app.menu_height
            text: '<'

        CheckBox:
            id: cb_abs
            group: 'g2'
            active: True
            on_active: root.on_checkbox2_active(self, '<')

        Label:
            text: '>'

        CheckBox:
            id: cb_close
            group: 'g2'
            on_active: root.on_checkbox2_active(self, '>')

    Label:
        text_size: self.size
        halign: 'center'
        valign: 'middle'
        id: lb_comment
        text: 'Price'


#
