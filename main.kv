#:kivy 1.10.0


<NoteScreen>:
    view: view

    BoxLayout:
        orientation: 'vertical'

        BoxLayout:
            size_hint: (1, 0.1)

            Button:
                text: "<<"
                size_hint_x: None
                width: self.height
                on_release: app.listv.edit_note(textinp.text)

            TextInput:
                id: textinp
                text: 'None' if not app.listv.data else app.listv.data[app.listv.note_index]['title']
                #text: app.listv.note['title']
                multiline: False
                focus: True
                #on_text: app.listv.edit_note(self.text)

            Button:
                text: "x"
                size_hint_x: None
                width: self.height
                #text: str(app.listv.index)
                on_release: app.listv.del_note()

        ScrollView:
            id: view
            size_hint: (1, 1)



<ListScreen>:
    view: view

    BoxLayout:
        orientation: 'vertical'

        BoxLayout:
            #size: (64, 64)
            size_hint: (1, 0.1)

            Button:
                text: "$"
                size_hint_x: None
                width: self.height

            Label:
                text: "My Broker"
                #color: (1, 1, 1, 1)
                #background_color: (.3, .3, .3, 1)

            Button:
                text: "+"
                size_hint_x: None
                width: self.height
                on_release: root.add_note()

        ScrollView:
            id: view
            size_hint: (1, 1)


#
